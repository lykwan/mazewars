const NUM_ROWS = 8;
const NUM_COLS = 8;
const NUM_MAZE_ROWS =  NUM_ROWS * 2 - 1;
const NUM_MAZE_COLS = NUM_COLS * 2 - 1;
const ORIGINAL_TILE_WIDTH = 101;
const ORIGINAL_TILE_HEIGHT = 122;
const TILE_RATIO = (1 / 2);
const TILE_WIDTH = ORIGINAL_TILE_WIDTH * TILE_RATIO;
const TILE_HEIGHT = ORIGINAL_TILE_HEIGHT * TILE_RATIO;

const mapGrid = {
  GAME_WIDTH: NUM_MAZE_ROWS * TILE_WIDTH,
  // CHANGE TILE HEIGHT TO CHAR HEIGHT
  GAME_HEIGHT: NUM_MAZE_COLS * TILE_WIDTH + TILE_HEIGHT,
  NUM_ROWS: NUM_ROWS,
  NUM_COLS: NUM_COLS,
  NUM_MAZE_ROWS: NUM_MAZE_ROWS,
  NUM_MAZE_COLS: NUM_MAZE_COLS,
  TILE_WIDTH: TILE_WIDTH,
  TILE_HEIGHT: TILE_HEIGHT,
  PLAYER_WIDTH: 20,
  PLAYER_HEIGHT: 15,
  BALL_WIDTH: 25,
  BALL_HEIGHT: 25,
  DFS_WIDTH: 25,
  DFS_HEIGHT: 0.30 * 25,
  BFS_WIDTH: 20,
  BFS_HEIGHT: 0.70 * 20,
  TILE_Z: 0,
  WALL_Z: 0,
  ACTOR_Z: 1.5,
  CHAR_STEP: 10
};

const weaponTypes = {
  BFS: 'BFS',
  DFS: 'DFS'
};

const gameSettings = {
  WEAPON_RANGE: 10,
  BUFFER_DAMAGE_TIME: 1000,
  BUFFER_SHOOTING_TIME: 1500,
  WEAPON_SPAWN_TIME: 10000,
  DAMAGE_ANIMATION_TIME: 100,
  DAMAGE_DISAPPEAR_TIME: 1000,
  HP_DAMAGE: 10,
  GAME_DURATION: 2000, // 200
  COLORS: ['blue', 'red', 'yellow', 'green']
};

module.exports = {
  mapGrid: mapGrid,
  weaponTypes: weaponTypes,
  gameSettings: gameSettings
};
